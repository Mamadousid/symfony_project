{% extends "themes/base_admin.html.twig" %}

{% block title %}Accueil{% endblock %}

{% block description %}Accueil{% endblock %}

{% block main %}
    <h1 class="text-center my-3 display-5">Bienvenue !</h1>
    <h2 class="text-center">Espace Administration Du Restaurant {{app.user.firstName}} {{app.user.lastName}}</h2>
    <div class="container my-5 text-center ">
        <div class="row justify-content-center">
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none text-dark" href="{{path('admin_category_index')}}">
                        <div class="card-header">Les catégories</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{categories|length}}</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none text-dark" href="{{path('admin_product_index')}}">
                        <div class="card-header">Les produits</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{products|length}}</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none text-dark" href="{{path('admin_formule_index')}}">
                        <div class="card-header">Les formules</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{formules|length}}</p>
                    </div>
                </div>
            </div>
        <div class="row">
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none text-dark" href="{{path('admin_user_index')}}">
                        <div class="card-header">Les utilisateurs</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{users|length}}</p>
                    </div>
                </div>
            </div>
            <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none text-dark" href="{{path('admin_contact_index')}}">
                        <div class="card-header">Les contacts</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{contacts|length}}</p>
                    </div>
                </div>
            </div>
           <div class="col-md-3 d-flex justify-content-center align-items-center my-3">
                <div class="card text-bg-light my-3 w-100 h-100">
                    <a class="text-decoration-none text-dark" href="{{path('admin_table_index')}}">
                        <div class="card-header">Les tables</div>
                    </a>
                    <div class="card-body">
                        <p class="card-text">{{tables|length}}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}